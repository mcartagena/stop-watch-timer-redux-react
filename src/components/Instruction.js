import React from 'react'

class Instruction extends React.Component {
    render() {
        return (
            <p id="instruction">Click the button to stop the watch and change format</p>
        )
    }
}

export default Instruction